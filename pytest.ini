[pytest]
testpaths = tests
addopts = -ra
markers =
  hardware: requires ESP32 on serial (ESP32_PORT)
  qa_fast: requires firmware built with -DQA_FAST=1
  relay_pulse: energizes the relay coil (~10s)
